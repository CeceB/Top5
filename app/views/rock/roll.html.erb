<h1>Rock</h1>
<p>The Top Five</p>
<div class="row">
    <% @songs.each do |song| %>
      <div class="col-md-6">
        <div class="well">
          <h2><%= link_to song.name[0..15], song %>...</h2>
          by<h3><%= song.artist.name %></h3>
          <p>
          <% song.rating.times do %>
          <span class="glyphicon glyphicon-star"></span>
          <%end%>
        </p>
        </div>
      </div>
    <% end %>
  </div>
